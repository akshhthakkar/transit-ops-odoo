export function TripsPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-white mb-6">Trips</h1>
      {/* TODO: Trip list, create/dispatch/complete/cancel — Phase 3 */}
      <p className="text-gray-500">Trip engine (create, dispatch, complete, cancel) — Phase 3</p>
    </div>
  );
}
