export function ReportsPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-white mb-6">Reports</h1>
      {/* TODO: KPI tiles, per-vehicle report, ROI, CSV export — Phase 5 */}
      <p className="text-gray-500">Fleet reports + CSV export — Phase 5</p>
    </div>
  );
}
